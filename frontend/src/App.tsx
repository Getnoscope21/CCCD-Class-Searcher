import { createContext, useCallback, useContext, useEffect, useMemo, useRef, useState, type FormEvent, type ReactNode } from 'react';
import { createClient, type Session, type SupabaseClient, type User } from '@supabase/supabase-js';
import { api } from './api';
import type { AppTab, College, CollegeCode, CourseCard, CourseDetail, CourseTab, Instructor, PlannerCourse, PlannerPlan, PlannerTerm, Requirement, Theme } from './types';

const COLLEGE_NAMES: Record<CollegeCode, string> = { GW: 'Golden West', OC: 'Orange Coast', CL: 'Coastline' };
const THEME_STORAGE_KEY = 'cccd-theme';
const ALL_STATUSES = ['OPEN', 'Waitlisted', 'CLOSED'];

function errorText(error: unknown) { return error instanceof Error ? error.message : 'Something went wrong. Please try again.'; }
function collegeName(code: CollegeCode) { return COLLEGE_NAMES[code] || code; }
function rmpUrl(name: string) { return `https://www.ratemyprofessors.com/search/professors?q=${encodeURIComponent(name)}`; }
function plural(value: number, singular: string) { return `${value} ${singular}${value === 1 ? '' : 's'}`; }

type AuthContextValue = { client: SupabaseClient | null; user: User | null; loading: boolean; signOut: () => Promise<void> };
const AuthContext = createContext<AuthContextValue>({ client: null, user: null, loading: true, signOut: async () => undefined });
function useAuth() { return useContext(AuthContext); }

function AuthProvider({ children }: { children: ReactNode }) {
  const [client, setClient] = useState<SupabaseClient | null>(null);
  const [user, setUser] = useState<User | null>(null);
  const [loading, setLoading] = useState(true);
  useEffect(() => {
    let active = true;
    let unsubscribe: (() => void) | undefined;
    void api.config().then(async (config) => {
      if (!active || !config.supabaseUrl || !config.supabaseAnonKey) return;
      const nextClient = createClient(config.supabaseUrl, config.supabaseAnonKey);
      setClient(nextClient);
      const { data } = await nextClient.auth.getSession();
      if (active) setUser(data.session?.user ?? null);
      const listener = nextClient.auth.onAuthStateChange((_event, session) => setUser(session?.user ?? null));
      unsubscribe = () => listener.data.subscription.unsubscribe();
    }).catch(() => undefined).finally(() => { if (active) setLoading(false); });
    return () => { active = false; unsubscribe?.(); };
  }, []);
  const signOut = useCallback(async () => { if (client) await client.auth.signOut(); }, [client]);
  return <AuthContext.Provider value={{ client, user, loading, signOut }}>{children}</AuthContext.Provider>;
}

function ThemeToggle() {
  const [theme, setTheme] = useState<Theme>(() => document.documentElement.dataset.theme === 'dark' ? 'dark' : 'light');
  useEffect(() => {
    const media = window.matchMedia('(prefers-color-scheme: dark)');
    const update = () => {
      try { if (!localStorage.getItem(THEME_STORAGE_KEY)) setTheme(media.matches ? 'dark' : 'light'); } catch { /* ignore */ }
    };
    media.addEventListener('change', update); return () => media.removeEventListener('change', update);
  }, []);
  useEffect(() => { document.documentElement.dataset.theme = theme; }, [theme]);
  const switchTheme = () => {
    const next = theme === 'dark' ? 'light' : 'dark';
    try { localStorage.setItem(THEME_STORAGE_KEY, next); } catch { /* ignore */ }
    setTheme(next);
  };
  const nextTheme = theme === 'dark' ? 'light' : 'dark';
  return <button className="theme-toggle" type="button" aria-pressed={theme === 'dark'} aria-label={`Switch to ${nextTheme} mode`} title={`Switch to ${nextTheme} mode`} onClick={switchTheme}><span className="theme-toggle-icon" aria-hidden="true" /></button>;
}

function Modal({ children, className = '', onClose }: { children: ReactNode; className?: string; onClose: () => void }) {
  return <div className="modal-overlay" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><div className={`modal ${className}`} role="dialog" aria-modal="true">{children}</div></div>;
}
function RatingSummary({ avg_rating, rating_count }: { avg_rating: number | null; rating_count: number }) {
  return <span className="rating">{avg_rating != null ? <><span className="stars">★ {avg_rating}</span> <span>({rating_count})</span></> : <span className="no-rating">No ratings yet</span>}</span>;
}
function StatusBadge({ status }: { status: string }) {
  const normalized = status.toLowerCase(); const kind = normalized.includes('open') ? 'open' : normalized.includes('closed') ? 'closed' : normalized.includes('wait') ? 'waitlist' : '';
  return <span className={`badge ${kind}`}>{status || '—'}</span>;
}
function ModalityBadge({ modality }: { modality: string }) { return <span className={`badge modality-${(modality || 'tba').toLowerCase().replace(/\s+/g, '-')}`}>{modality || 'TBA'}</span>; }

function AuthDialog({ mode, onClose }: { mode: 'signin' | 'signup'; onClose: () => void }) {
  const { client } = useAuth(); const [authMode, setAuthMode] = useState(mode); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [notice, setNotice] = useState(''); const [busy, setBusy] = useState(false);
  const submit = async (event: FormEvent) => {
    event.preventDefault(); if (!client) return; setError(''); setNotice('');
    if (!email || !password) { setError('Enter an email and password.'); return; }
    setBusy(true);
    if (authMode === 'signup') { const { data, error: authError } = await client.auth.signUp({ email, password }); if (authError) setError(authError.message); else if (data.session) onClose(); else setNotice('Check your email to confirm your account, then sign in.'); }
    else { const { error: authError } = await client.auth.signInWithPassword({ email, password }); if (authError) setError(authError.message); else onClose(); }
    setBusy(false);
  };
  return <Modal onClose={onClose}><h3>{authMode === 'signup' ? 'Sign up' : 'Sign in'}</h3>{error && <p className="auth-error">{error}</p>}{notice && <p className="auth-notice">{notice}</p>}<form onSubmit={submit}><input value={email} onChange={(e) => setEmail(e.target.value)} type="email" placeholder="Email" className="auth-input" autoComplete="email" /><input value={password} onChange={(e) => setPassword(e.target.value)} type="password" placeholder="Password" className="auth-input" autoComplete="current-password" /><div className="modal-actions"><button type="button" className="btn-secondary" onClick={onClose}>Cancel</button><button className="btn-primary" disabled={busy}>{busy ? 'Please wait…' : authMode === 'signup' ? 'Sign up' : 'Sign in'}</button></div></form><p className="modal-note"><button className="text-button" onClick={() => { setAuthMode(authMode === 'signin' ? 'signup' : 'signin'); setError(''); setNotice(''); }}>{authMode === 'signup' ? 'Already have an account? Sign in' : "Don't have an account? Sign up"}</button></p></Modal>;
}

function RatingDialog({ instructor, college, onClose, onRated }: { instructor: string; college: CollegeCode; onClose: () => void; onRated: () => void }) {
  const [rating, setRating] = useState(0); const [comment, setComment] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  const submit = async () => { if (!rating) { setError('Pick a star rating first.'); return; } setBusy(true); setError(''); try { await api.addRating({ instructor, college, rating, comment }); onRated(); onClose(); } catch (err) { setError(errorText(err)); } finally { setBusy(false); } };
  return <Modal onClose={onClose}><h3>Rate {instructor}</h3>{error && <p className="auth-error">{error}</p>}<div className="star-picker" aria-label="Rating out of five">{[1, 2, 3, 4, 5].map((value) => <button key={value} type="button" className={value <= rating ? 'filled' : ''} aria-label={`${value} star${value === 1 ? '' : 's'}`} onClick={() => setRating(value)}>★</button>)}</div><textarea value={comment} onChange={(e) => setComment(e.target.value)} placeholder="Optional comment (visible to other students on this site)" maxLength={500} /><div className="modal-actions"><button className="btn-secondary" onClick={onClose}>Cancel</button><button className="btn-primary" disabled={busy} onClick={submit}>{busy ? 'Submitting…' : 'Submit rating'}</button></div><p className="modal-note">Ratings here are submitted by users of this site directly — not pulled from any other site.</p></Modal>;
}

function ClassesPage({ colleges, requirements, onOpenCourse, onRate }: { colleges: College[]; requirements: Requirement[]; onOpenCourse: (course: CourseCard) => void; onRate: (instructor: string, college: CollegeCode, refresh: () => void) => void }) {
  const [filters, setFilters] = useState({ q: '', college: '', requirement: '', modality: '', min: 0, max: 5, statuses: ALL_STATUSES, sort: 'relevance' }); const [cards, setCards] = useState<CourseCard[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const timer = useRef<number | undefined>(undefined);
  const search = useCallback(async () => { setLoading(true); setError(''); const params = new URLSearchParams(); if (filters.q.trim()) params.set('q', filters.q.trim()); if (filters.college) params.set('college', filters.college); if (filters.requirement) params.set('requirement', filters.requirement); if (filters.modality) params.set('modality', filters.modality); if (filters.sort) params.set('sort', filters.sort); if (filters.min > 0) params.set('units_min', String(filters.min)); if (filters.max < 5) params.set('units_max', String(filters.max)); if (filters.statuses.length < ALL_STATUSES.length) params.set('statuses', filters.statuses.join(',')); try { setCards(await api.courseCards(params)); } catch (err) { setError(errorText(err)); } finally { setLoading(false); } }, [filters]);
  useEffect(() => { window.clearTimeout(timer.current); timer.current = window.setTimeout(() => void search(), 250); return () => window.clearTimeout(timer.current); }, [search]);
  const update = <K extends keyof typeof filters>(key: K, value: (typeof filters)[K]) => setFilters((current) => ({ ...current, [key]: value }));
  const statusChange = (status: string, checked: boolean) => update('statuses', checked ? [...filters.statuses, status] : filters.statuses.filter((item) => item !== status));
  const rangePercent = { left: `${filters.min / 5 * 100}%`, right: `${100 - filters.max / 5 * 100}%` };
  return <div className="classes-layout"><aside className="sidebar"><h3 className="sidebar-title">Filters</h3><div className="sidebar-block"><input value={filters.q} onChange={(e) => update('q', e.target.value)} type="text" placeholder="Search course, title, CRN, or instructor..." /></div><div className="sidebar-block"><h4>College</h4><select value={filters.college} onChange={(e) => update('college', e.target.value)}><option value="">All Colleges</option>{colleges.map((college) => <option key={college.code} value={college.code}>{college.name}</option>)}</select></div><div className="sidebar-block"><h4>Requirement</h4><select value={filters.requirement} onChange={(e) => update('requirement', e.target.value)}><option value="">Any Requirement</option>{requirements.map((requirement) => <option key={requirement.key} value={requirement.key}>{requirement.label}</option>)}</select></div><div className="sidebar-block"><h4>Modality</h4><select value={filters.modality} onChange={(e) => update('modality', e.target.value)}><option value="">Any Modality</option>{['In-Person', 'Online', 'Live Online', 'TBA'].map((modality) => <option key={modality}>{modality}</option>)}</select></div><div className="sidebar-block"><h4>Units</h4><div className="unit-slider"><div className="unit-slider-track" /><div className="unit-slider-range" style={rangePercent} /><input type="range" min="0" max="5" step="0.5" value={filters.min} onChange={(e) => update('min', Math.min(Number(e.target.value), filters.max))} /><input type="range" min="0" max="5" step="0.5" value={filters.max} onChange={(e) => update('max', Math.max(Number(e.target.value), filters.min))} /></div><div className="unit-slider-labels"><span>{filters.min}</span><span>–</span><span>{filters.max >= 5 ? '5+' : filters.max}</span><span>units</span></div></div><div className="sidebar-block"><h4>Enrollment status</h4>{ALL_STATUSES.map((status) => <label className="checkbox" key={status}><input type="checkbox" checked={filters.statuses.includes(status)} onChange={(e) => statusChange(status, e.target.checked)} />{status === 'Waitlisted' ? 'Waitlisted' : status[0] + status.slice(1).toLowerCase()}</label>)}</div><div className="sidebar-block"><h4>Sort by</h4><select value={filters.sort} onChange={(e) => update('sort', e.target.value)}>{[['relevance', 'Relevance'], ['units', 'Units'], ['rating', 'Rating'], ['seats', 'Seats available'], ['requirements', 'Requirements'], ['semester', 'Semester'], ['datetime', 'Date & time']].map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></div></aside><div className="cards-col"><div className="status"><span>{loading ? 'Searching…' : `${plural(cards.length, 'course')}${cards.length === 300 ? ' (showing first 300 — narrow your search)' : ''}`}</span>{error && <span className="inline-error">{error} <button className="text-button" onClick={() => void search()}>Retry</button></span>}</div>{!loading && !error && cards.length === 0 && <div className="planner-empty">No courses match these filters.</div>}<div className="course-cards">{cards.map((course) => <CourseCardView key={`${course.college}-${course.subject}-${course.course_number}`} course={course} onClick={() => onOpenCourse(course)} />)}</div></div></div>;
}

function CourseCardView({ course, onClick }: { course: CourseCard; onClick: () => void }) { const units = course.units_min === course.units_max ? course.units_min : `${course.units_min}–${course.units_max}`; return <button className="course-card" onClick={onClick}><div className="course-card-top"><span className="course-card-code">{course.subject} {course.course_number}</span><span className="badge">{units} unit{course.units_max === 1 ? '' : 's'}</span></div><div className="course-card-title">{course.title}</div><div className="course-card-college">{collegeName(course.college)}</div>{course.description && <p className="course-card-desc">{course.description}</p>}<div className="course-card-bottom"><RatingSummary {...course} /><span className="course-card-sections">{plural(course.section_count, 'section')}</span></div><div className="course-card-bottom">{course.seats_available > 0 ? <span className="badge open">{plural(course.seats_available, 'seat')} open</span> : <span className="badge closed">Full</span>}{course.requirement_count > 0 && <span className="badge modality-in-person">{plural(course.requirement_count, 'requirement')}</span>}</div></button>; }

function ProfessorsPage({ colleges, onRate }: { colleges: College[]; onRate: (instructor: string, college: CollegeCode, refresh: () => void) => void }) {
  const [q, setQ] = useState(''); const [college, setCollege] = useState(''); const [rows, setRows] = useState<Instructor[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const timer = useRef<number | undefined>(undefined);
  const search = useCallback(async () => { setLoading(true); setError(''); const params = new URLSearchParams(); if (q.trim()) params.set('q', q.trim()); if (college) params.set('college', college); try { setRows(await api.instructors(params)); } catch (err) { setError(errorText(err)); } finally { setLoading(false); } }, [q, college]);
  useEffect(() => { window.clearTimeout(timer.current); timer.current = window.setTimeout(() => void search(), 250); return () => window.clearTimeout(timer.current); }, [search]);
  return <><div className="filters"><input value={q} onChange={(e) => setQ(e.target.value)} type="text" placeholder="Search professor name..." /><select value={college} onChange={(e) => setCollege(e.target.value)}><option value="">All Colleges</option>{colleges.map((item) => <option key={item.code} value={item.code}>{item.name}</option>)}</select></div><div className="status">{loading ? 'Searching…' : `${plural(rows.length, 'instructor')}`}{error && <span className="inline-error">{error} <button className="text-button" onClick={() => void search()}>Retry</button></span>}</div><div className="table-wrap"><table><thead><tr><th>Instructor</th><th>College</th><th>Sections</th><th>Rating</th><th>RateMyProfessor</th></tr></thead><tbody>{rows.map((row) => <tr key={`${row.instructor}-${row.college}`}><td>{row.instructor}</td><td>{collegeName(row.college)}</td><td>{row.section_count}</td><td><RatingSummary {...row} /> <button className="rate-btn" onClick={() => onRate(row.instructor, row.college, () => void search())}>Rate</button></td><td><a className="rmp-link" href={row.rmp_search_url} target="_blank" rel="noreferrer">Search on RateMyProfessor →</a></td></tr>)}</tbody></table></div></>;
}

function CourseDetailDialog({ course, onClose, onRate, onRefresh }: { course: Pick<CourseCard, 'college' | 'subject' | 'course_number'>; onClose: () => void; onRate: (instructor: string, college: CollegeCode, refresh: () => void) => void; onRefresh: () => void }) {
  const { user } = useAuth(); const [detail, setDetail] = useState<CourseDetail | null>(null); const [error, setError] = useState(''); const [tab, setTab] = useState<CourseTab>('overview'); const [note, setNote] = useState(''); const [savingNote, setSavingNote] = useState(false); const [menuOpen, setMenuOpen] = useState(false); const [plan, setPlan] = useState<PlannerPlan>({ terms: [] }); const [plannerError, setPlannerError] = useState('');
  const { loadPlan, addCourse } = usePlanner();
  const load = useCallback(async () => { setError(''); try { setDetail(await api.course(course.college, course.subject, course.course_number)); } catch (err) { setError(errorText(err)); } }, [course]);
  useEffect(() => { void load(); }, [load]);
  const openPlanner = async () => { if (!user) return; try { setPlan(await loadPlan()); setPlannerError(''); setMenuOpen(true); } catch (err) { setPlannerError(errorText(err)); setMenuOpen(true); } };
  const saveNote = async () => { if (!note.trim()) return; setSavingNote(true); try { const response = await api.addRequirement({ college: course.college, subject: course.subject, course_number: course.course_number, text: note.trim() }); setDetail((current) => current ? { ...current, requirements: response.requirements } : current); setNote(''); onRefresh(); } catch (err) { setError(errorText(err)); } finally { setSavingNote(false); } };
  const plannedCourse: PlannerCourse | null = detail ? { college: detail.college, subject: detail.subject, course_number: detail.course_number, title: detail.title, units: detail.sections[0] ? Number(detail.sections[0].credits) : null } : null;
  return <Modal className="course-modal" onClose={onClose}><button className="modal-close" aria-label="Close" onClick={onClose}>×</button><div className="course-modal-header"><h2>{detail ? `${detail.subject} ${detail.course_number} — ${detail.title || ''}` : `${course.subject} ${course.course_number}`}</h2><p className="course-modal-subtitle">{detail ? `${collegeName(detail.college)} · ${detail.term_desc || ''}` : 'Loading…'}</p><div className="add-to-plan">{user && <button className="btn-secondary" onClick={() => void openPlanner()}>+ Add to Plan</button>}{menuOpen && <div className="add-to-plan-menu">{plannerError ? <div className="add-to-plan-empty">{plannerError}</div> : plan.terms.length ? plan.terms.map((term) => { const added = term.courses.some((item) => item.college === course.college && item.subject === course.subject && item.course_number === course.course_number); return <button className="add-to-plan-option" key={term.id} disabled={added || !plannedCourse} onClick={() => { if (plannedCourse) void addCourse(term.id, plannedCourse).then(() => setMenuOpen(false)); }}><span>{term.label}</span>{added && <span className="added-check">Added ✓</span>}</button>; }) : <div className="add-to-plan-empty">No semesters yet — go to the Planner tab to add one.</div>}</div>}</div></div><div className="course-modal-tabs">{(['overview', 'sections', 'requirements'] as CourseTab[]).map((name) => <button key={name} className={`course-tab-btn ${tab === name ? 'active' : ''}`} onClick={() => setTab(name)}>{name[0].toUpperCase() + name.slice(1)}</button>)}</div><div className="course-modal-body">{error && <p className="auth-error">{error}</p>}{!detail && !error && <p className="no-rating">Loading course details…</p>}{detail && tab === 'overview' && <div>{detail.description ? <p>{detail.description}</p> : <p className="no-rating">No description available.</p>}{detail.corequisites && <p><strong>Corequisites:</strong> {detail.corequisites}</p>}{detail.transfer_credit && <p><strong>Transfer credit:</strong> {detail.transfer_credit}</p>}{detail.assist_url && <p><a className="rmp-link" href={detail.assist_url} target="_blank" rel="noreferrer">See what {collegeName(detail.college)} courses transfer as on ASSIST.org →</a></p>}</div>}{detail && tab === 'sections' && <div className="section-cards">{detail.sections.map((section) => <div className="section-card" key={section.crn}><div className="section-card-top"><span className="section-card-crn">CRN {section.crn}</span><ModalityBadge modality={section.modality} /></div><div className="section-card-instructor">{section.instructor || '—'}</div>{section.instructor && <><div className="section-card-rating"><RatingSummary {...section} /> <button className="rate-btn" onClick={() => onRate(section.instructor, section.college, () => void load())}>Rate</button></div><div className="section-card-links"><a className="rmp-link" href={rmpUrl(section.instructor)} target="_blank" rel="noreferrer">Search on RateMyProfessor →</a></div></>}<div className="section-card-row"><span className="label">Meeting:</span>{section.meeting_info || '—'}</div><div className="section-card-row"><span className="label">Location:</span>{section.location || '—'}</div><div className="section-card-bottom"><span className="section-card-row"><span className="label">Seats:</span>{section.cap != null ? `${section.act}/${section.cap}` : '—'}</span><StatusBadge status={section.status} /></div></div>)}</div>}{detail && tab === 'requirements' && <div><div className="requirements-list">{detail.requirements.length ? detail.requirements.map((requirement) => <div className="requirement-item" key={requirement.id}><p>{requirement.requirement_text}</p><span className="requirement-date">{new Date(`${requirement.created_at}Z`).toLocaleDateString()}</span></div>) : <p className="no-rating">No requirements submitted yet — be the first.</p>}</div><div className="requirement-form"><textarea value={note} onChange={(e) => setNote(e.target.value)} placeholder="Add a requirement or note for students (e.g. prior coursework, materials needed)..." maxLength={500} /><button className="btn-primary" disabled={savingNote || !note.trim()} onClick={() => void saveNote()}>{savingNote ? 'Saving…' : 'Add requirement'}</button></div><p className="modal-note">Requirements here are submitted directly by users of this site.</p></div>}</div></Modal>;
}

type PlannerContextValue = { plan: PlannerPlan; loading: boolean; error: string; loadPlan: () => Promise<PlannerPlan>; addSemester: (label: string) => Promise<void>; removeSemester: (id: string) => Promise<void>; renameSemester: (id: string, label: string) => Promise<void>; addCourse: (termId: string, course: PlannerCourse) => Promise<void>; removeCourse: (termId: string, course: PlannerCourse) => Promise<void> };
const PlannerContext = createContext<PlannerContextValue | null>(null);
function usePlanner() { const context = useContext(PlannerContext); if (!context) throw new Error('Planner provider missing'); return context; }
function PlannerProvider({ children }: { children: ReactNode }) {
  const { client, user } = useAuth(); const [plan, setPlan] = useState<PlannerPlan>({ terms: [] }); const [loading, setLoading] = useState(false); const [error, setError] = useState('');
  const loadPlan = useCallback(async () => { if (!client || !user) { const empty = { terms: [] }; setPlan(empty); return empty; } setLoading(true); setError(''); try { const [{ data: terms, error: termsError }, { data: courses, error: coursesError }] = await Promise.all([client.from('planner_terms').select('*').order('position'), client.from('planner_courses').select('*')]); if (termsError || coursesError) throw termsError || coursesError; const next = { terms: (terms || []).map((term) => ({ id: term.id as string, label: term.label as string, position: term.position as number, courses: (courses || []).filter((course) => course.term_id === term.id).map((course) => ({ college: course.college as CollegeCode, subject: course.subject as string, course_number: course.course_number as string, title: course.title as string | null, units: course.units == null ? null : Number(course.units) })) })) }; setPlan(next); return next; } catch (err) { setError(errorText(err)); throw err; } finally { setLoading(false); } }, [client, user]);
  useEffect(() => { void loadPlan().catch(() => undefined); }, [loadPlan]);
  const mutate = async (action: () => Promise<void>) => { setError(''); try { await action(); await loadPlan(); } catch (err) { setError(errorText(err)); throw err; } };
  const addSemester = (label: string) => mutate(async () => { if (!client || !user) return; const position = plan.terms.length ? Math.max(...plan.terms.map((term) => term.position ?? 0)) + 1 : 0; const { error: insertError } = await client.from('planner_terms').insert({ user_id: user.id, label, position }); if (insertError) throw insertError; });
  const removeSemester = (id: string) => mutate(async () => { if (!client) return; const { error: deleteError } = await client.from('planner_terms').delete().eq('id', id); if (deleteError) throw deleteError; });
  const renameSemester = (id: string, label: string) => mutate(async () => { if (!client || !label) return; const { error: updateError } = await client.from('planner_terms').update({ label }).eq('id', id); if (updateError) throw updateError; });
  const addCourse = (termId: string, course: PlannerCourse) => mutate(async () => { if (!client || !user) return; const { error: upsertError } = await client.from('planner_courses').upsert({ term_id: termId, user_id: user.id, ...course }, { onConflict: 'term_id,college,subject,course_number' }); if (upsertError) throw upsertError; });
  const removeCourse = (termId: string, course: PlannerCourse) => mutate(async () => { if (!client) return; const { error: deleteError } = await client.from('planner_courses').delete().eq('term_id', termId).eq('college', course.college).eq('subject', course.subject).eq('course_number', course.course_number); if (deleteError) throw deleteError; });
  return <PlannerContext.Provider value={{ plan, loading, error, loadPlan, addSemester, removeSemester, renameSemester, addCourse, removeCourse }}>{children}</PlannerContext.Provider>;
}

function PlannerPage({ onSignIn }: { onSignIn: () => void }) {
  const { user, loading: authLoading } = useAuth(); const { plan, loading, error, addSemester, removeSemester, renameSemester, removeCourse } = usePlanner(); const [busy, setBusy] = useState(false);
  const add = async () => { if (!user) { onSignIn(); return; } setBusy(true); try { await addSemester(`Semester ${plan.terms.length + 1}`); } finally { setBusy(false); } };
  const total = plan.terms.reduce((sum, term) => sum + term.courses.reduce((units, course) => units + (course.units || 0), 0), 0);
  return <section><div className="planner-header"><div><h2 className="planner-title">Your Plan</h2><p className="planner-subtitle">{user ? "Add classes to a semester from any course's Overview tab." : 'Sign in to create and save your plan.'}</p></div><div className="planner-header-right">{user && plan.terms.length > 0 && <span className="planner-total">{plural(total, 'total unit')} planned</span>}<button className="btn-primary" disabled={authLoading || busy} onClick={() => void add()}>{busy ? 'Adding…' : '+ Add Semester'}</button></div></div>{!authLoading && !user && <div className="planner-empty"><strong>Sign in</strong> to create and save your plan — it’s tied to your account, so it’s there whenever and wherever you log back in.</div>}{error && <p className="auth-error">{error}</p>}{user && !loading && plan.terms.length === 0 && <div className="planner-empty">Nothing planned yet. Click <strong>+ Add Semester</strong> to start, then add classes from their Overview tab.</div>}<div className="planner-terms">{plan.terms.map((term) => <PlannerTermView key={term.id} term={term} onRename={renameSemester} onRemove={removeSemester} onRemoveCourse={removeCourse} />)}</div></section>;
}

function PlannerTermView({ term, onRename, onRemove, onRemoveCourse }: { term: PlannerTerm; onRename: (id: string, label: string) => Promise<void>; onRemove: (id: string) => Promise<void>; onRemoveCourse: (id: string, course: PlannerCourse) => Promise<void> }) { const [label, setLabel] = useState(term.label); const units = term.courses.reduce((sum, course) => sum + (course.units || 0), 0); return <div className="planner-term"><div className="planner-term-top"><input className="planner-term-title" value={label} onChange={(event) => setLabel(event.target.value)} onBlur={() => { if (label.trim() && label !== term.label) void onRename(term.id, label.trim()); }} /><button className="planner-term-remove" aria-label="Remove semester" onClick={() => void onRemove(term.id)}>×</button></div><div className="planner-term-units">{plural(units, 'unit')}</div><div className="planner-course-list">{term.courses.length ? term.courses.map((course) => <div className="planner-course-item" key={`${course.college}-${course.subject}-${course.course_number}`}><div className="planner-course-info"><div className="planner-course-code">{course.subject} {course.course_number} <span className="planner-course-units">({course.units ?? '—'} units)</span></div><div className="planner-course-title">{course.title || ''} · {collegeName(course.college)}</div></div><button className="planner-course-remove" aria-label="Remove class" onClick={() => void onRemoveCourse(term.id, course)}>×</button></div>) : <div className="planner-term-empty">No classes added yet.</div>}</div></div>; }

function AppContent() {
  const { user, signOut } = useAuth(); const [tab, setTab] = useState<AppTab>('classes'); const [colleges, setColleges] = useState<College[]>([]); const [requirements, setRequirements] = useState<Requirement[]>([]); const [startupError, setStartupError] = useState(''); const [course, setCourse] = useState<CourseCard | null>(null); const [authMode, setAuthMode] = useState<'signin' | 'signup' | null>(null); const [rating, setRating] = useState<{ instructor: string; college: CollegeCode; refresh: () => void } | null>(null);
  useEffect(() => { void Promise.all([api.colleges(), api.requirements()]).then(([nextColleges, nextRequirements]) => { setColleges(nextColleges); setRequirements(nextRequirements); }).catch((err) => setStartupError(errorText(err))); }, []);
  return <><header><div className="header-account">{user ? <><span id="account-email">{user.email}</span><button className="header-account-btn" onClick={() => void signOut()}>Sign out</button></> : <button className="header-account-btn" onClick={() => setAuthMode('signin')}>Sign in</button>}<ThemeToggle /></div><h1>Coast Colleges Class Finder</h1><p className="subtitle">Golden West · Orange Coast · Coastline</p></header><main>{startupError && <p className="auth-error">{startupError}</p>}<div className="tabs">{(['classes', 'professors', 'planner'] as AppTab[]).map((name) => <button className={`tab-btn ${tab === name ? 'active' : ''}`} key={name} onClick={() => setTab(name)}>{name[0].toUpperCase() + name.slice(1)}</button>)}</div>{tab === 'classes' && <ClassesPage colleges={colleges} requirements={requirements} onOpenCourse={setCourse} onRate={(instructor, college, refresh) => setRating({ instructor, college, refresh })} />}{tab === 'professors' && <ProfessorsPage colleges={colleges} onRate={(instructor, college, refresh) => setRating({ instructor, college, refresh })} />}{tab === 'planner' && <PlannerPage onSignIn={() => setAuthMode('signin')} />}</main>{course && <CourseDetailDialog course={course} onClose={() => setCourse(null)} onRate={(instructor, college, refresh) => setRating({ instructor, college, refresh })} onRefresh={() => undefined} />}{authMode && <AuthDialog mode={authMode} onClose={() => setAuthMode(null)} />}{rating && <RatingDialog instructor={rating.instructor} college={rating.college} onClose={() => setRating(null)} onRated={rating.refresh} />}<footer><p>Class data pulled from the official CCCD public schedule search. Not affiliated with Golden West College, Orange Coast College, Coastline Community College, or the Coast Community College District. Ratings are user-submitted on this site and are not sourced from RateMyProfessor or any third party.</p></footer></>;
}

export default function App() { return <AuthProvider><PlannerProvider><AppContent /></PlannerProvider></AuthProvider>; }
